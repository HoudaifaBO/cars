<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= car.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <!-- Include navigation bar partial -->
    <%- include('partials/navbar') %>

    <div class="container mt-4">
        <div class="row">
            <div class="col-md-12 mb-3">
                <a href="/cars/<%= car.id %>" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Terug naar auto details
                </a>
            </div>
        </div>

        <div class="card">
            <div class="card-header">
                <h1>Bewerk <%= car.name %></h1>
            </div>
            <div class="card-body">
                <form action="/cars/<%= car.id %>/edit" method="POST">
                    <div class="mb-3">
                        <label for="name" class="form-label">Naam</label>
                        <input type="text" class="form-control" id="name" name="name" value="<%= car.name %>" required>
                    </div>

                    <div class="mb-3">
                        <label for="price" class="form-label">Prijs</label>
                        <input type="number" class="form-control" id="price" name="price" value="<%= car.price %>" step="0.01" required>
                    </div>

                    <div class="mb-3">
                        <label for="category" class="form-label">Categorie</label>
                        <select class="form-select" id="category" name="category" required>
                            <option value="">Selecteer een categorie</option>
                            <option value="Sportwagen" <%= car.category === 'Sportwagen' ? 'selected' : '' %>>Sportwagen</option>
                            <option value="SUV" <%= car.category === 'SUV' ? 'selected' : '' %>>SUV</option>
                            <option value="Sedan" <%= car.category === 'Sedan' ? 'selected' : '' %>>Sedan</option>
                            <option value="Hatchback" <%= car.category === 'Hatchback' ? 'selected' : '' %>>Hatchback</option>
                            <option value="Coupe" <%= car.category === 'Coupe' ? 'selected' : '' %>>Coupé</option>
                            <option value="Cabriolet" <%= car.category === 'Cabriolet' ? 'selected' : '' %>>Cabriolet</option>
                            <option value="Hybride" <%= car.category === 'Hybride' ? 'selected' : '' %>>Hybride</option>
                            <option value="Elektrisch" <%= car.category === 'Elektrisch' ? 'selected' : '' %>>Elektrisch</option>
                        </select>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Beschikbaarheid</label>
                        <div class="form-check">
                            <input class="form-check-input" type="radio" name="isAvailable" id="availableYes" value="true" <%= car.isAvailable ? 'checked' : '' %>>
                            <label class="form-check-label" for="availableYes">
                                Beschikbaar
                            </label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input" type="radio" name="isAvailable" id="availableNo" value="false" <%= !car.isAvailable ? 'checked' : '' %>>
                            <label class="form-check-label" for="availableNo">
                                Niet beschikbaar
                            </label>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label for="description" class="form-label">Beschrijving</label>
                        <textarea class="form-control" id="description" name="description" rows="4"><%= car.description || '' %></textarea>
                    </div>

                    <div class="mb-3">
                        <label for="features" class="form-label">Features (comma gescheiden)</label>
                        <textarea class="form-control" id="features" name="features" rows="3"><%= car.features ? car.features.join(', ') : '' %></textarea>
                        <div class="form-text">Voer features in gescheiden door komma's, bijvoorbeeld: "ABS, Airconditioning, Navigatie"</div>
                    </div>

                    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                        <a href="/cars/<%= car.id %>" class="btn btn-secondary me-md-2">Annuleren</a>
                        <button type="submit" class="btn btn-primary">Opslaan</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>