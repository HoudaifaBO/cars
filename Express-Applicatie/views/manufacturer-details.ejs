
<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= manufacturer.name %> - Fabrikant Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <!-- Include navigation bar partial -->
    <%- include('partials/navbar') %>

    <div class="container mt-4">
        <div class="row">
            <div class="col-md-12 mb-3">
                <a href="/manufacturers" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Terug naar fabrikanten
                </a>
            </div>
        </div>

        <div class="row">
            <!-- Manufacturer Logo Column -->
            <div class="col-md-4">
                <% if(manufacturer.logoUrl) { %>
                    <img src="<%= manufacturer.logoUrl %>" alt="<%= manufacturer.name %>" class="img-fluid rounded mb-3">
                <% } else { %>
                    <div class="bg-light rounded d-flex align-items-center justify-content-center" style="height: 200px">
                        <p class="text-muted">Geen logo beschikbaar</p>
                    </div>
                <% } %>
            </div>
            
            <!-- Manufacturer Details Column -->
            <div class="col-md-8">
                <h1><%= manufacturer.name %></h1>
                
                <div class="card mb-4">
                    <div class="card-header">
                        <h3>Fabrikant Gegevens</h3>
                    </div>
                    <div class="card-body">
                        <table class="table table-striped">
                            <tbody>
                                <tr>
                                    <th>ID:</th>
                                    <td><%= manufacturer.id %></td>
                                </tr>
                                <tr>
                                    <th>Naam:</th>
                                    <td><%= manufacturer.name %></td>
                                </tr>
                                <tr>
                                    <th>Oprichter:</th>
                                    <td><%= manufacturer.founder || 'N/A' %></td>
                                </tr>
                                <tr>
                                    <th>Oprichtingsjaar:</th>
                                    <td><%= manufacturer.foundedYear || 'N/A' %></td>
                                </tr>
                                <tr>
                                    <th>Motto:</th>
                                    <td><%= manufacturer.motto || 'N/A' %></td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <!-- Description Section -->
        <div class="row mt-4">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">
                        <h3>Beschrijving</h3>
                    </div>
                    <div class="card-body">
                        <% if(manufacturer.description) { %>
                            <p><%= manufacturer.description %></p>
                        <% } else { %>
                            <p class="text-muted">Geen beschrijving beschikbaar.</p>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>